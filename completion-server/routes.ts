import { Router } from "express";
import { Request, Response } from "express";

export const completionServerRoutes = Router();

export function conversationRelayWebsocketHandler(
  ws: WebSocket,
  req: Request
) {}
